using UnityEngine;

public class WaveControlller : MonoBehaviour
{

    public LessonWave lessonWave;



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

        for (int i = 0; i < lessonWave.enemyCount; ++i)
        {

        }

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
